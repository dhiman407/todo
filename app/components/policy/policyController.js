'use strict';

define(['app'],function(app){

	app.register.controller('policyController',['$scope','$timeout','$filter','uiGridConstants',function($scope,$timeout,$filter,uiGridConstants){        

	}]);
});